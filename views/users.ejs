<!DOCTYPE html>
<html lang="en">
    
<%- include("./partials/head.ejs") %> 

<body>
    <% for ( let i = 0; i < resources.length; i++ ) { %>
        <h3> <%= resources[i].email %>  </h3>
        <!-- <p> <%= resources[i].from %>  </p> -->
    <% } %>
</body>
</html>